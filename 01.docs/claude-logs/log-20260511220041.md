# Claude Code Session Log - 2026-05-11 22:00:41

## User request
- Tham chiếu @docs/chatbot.md
- Yêu cầu: "Hãy implement chat bot cho tôi nào"
- Model: claude-opus-4-7 (vừa /model opus)

## Context cần lưu ý
- Stack thực tế: NestJS 10 + TypeORM + MySQL (backend, port 8080) + React 18 + Vite + Ant Design 5 (frontend, port 5173). KHÔNG phải NextJS như tài liệu mô tả.
- Multi-tenant: database-per-tenant. `TenantContextService` (AsyncLocalStorage) + `TenantDataSourceManager`.
- 3 layers: `platform/`, `tenant/`, `tenant-module/`. Không import chéo.
- Chưa có module chatbot/AI nào hiện tại.

## Plan đang đề xuất (chờ user xác nhận)
- Phase 1 (MVP): Backend chat endpoint streaming SSE + frontend chat widget (FAB + popover) + 1 tool đơn giản (vd `getProducts` hoặc RAG đơn giản qua MySQL fulltext). Chưa cần OpenSearch/embeddings.
- Phase 2: Thêm tools function calling cho invoices/customers/suppliers/debt/stock (tuỳ những module đã implement xong).
- Phase 3: RAG thật với vector store (OpenSearch trên Aiven hoặc giải pháp đơn giản hơn như pgvector / qdrant cloud).
- Phase 4: Agentic features (deep linking, action thực thi như tạo phiếu thu).

Lý do chia phase: scope tài liệu rất lớn, chi phí hạ tầng (OpenSearch) đáng kể; nên dùng phase 1 để verify UX + auth flow trước.

## Câu hỏi cần xác nhận
1. Provider LLM: OpenAI GPT-4o (như doc) hay Anthropic Claude? Có sẵn API key chưa?
2. Phase 1 trước hay đi thẳng full kiến trúc?
3. Cho phép cài thêm dependencies (npm i) ở backend & frontend?

## Actions

### User confirmed
- LLM: OpenAI GPT-4o mini
- Full kiến trúc (RAG + tools + agentic)
- OK install deps
- Yêu cầu thêm: rate limit 20k/day toàn hệ thống, 200/user/day; mute lịch sự khi vượt

### Đã làm xong
1. **docs/chatbot.md** — append section §4 Rate Limiting + §5 cấu trúc thư mục + §6 hướng dẫn setup + §7 API reference
2. **Backend deps**: `npm i openai @opensearch-project/opensearch` (vào `backend/package.json`)
3. **Frontend deps**: `npm i react-markdown`
4. **backend/src/config/chatbot.config.ts** — load env (model, OpenSearch, quota...). Đăng ký vào `app.module.ts`
5. **backend/src/tenant-module/chatbot/** — 8 files:
   - `chatbot.module.ts`
   - `chatbot.controller.ts` — `POST /api/tenant/chatbot/chat` (SSE) + `GET /status`
   - `chatbot.service.ts` — orchestration (quota check → multi-turn tool calling → stream)
   - `chatbot-quota.service.ts` — Upstash Redis counter (fallback in-mem nếu chưa cấu hình)
   - `chatbot-quota.exception.ts` — `USER_QUOTA_EXCEEDED` (429), `GLOBAL_QUOTA_EXCEEDED` (503)
   - `chatbot-tools.service.ts` — 9 tools function-calling (`getProducts`/`getCustomers`/`getSuppliers`/`getInvoices`/`getPurchaseInvoices`/`getStock`/`calculateCustomerDebt`/`calculateSupplierDebt`/`ragSearch`)
   - `openai.service.ts` — wrapper SDK (chat stream + embeddings)
   - `opensearch.service.ts` — KNN search với tenant filter, ensureIndex helper
   - `dto/chat.dto.ts`
   - `scripts/ingest-docs.ts` — CLI chunk + embed + upsert .md
6. **`tenant-app.module.ts`**: import `ChatbotModule`
7. **`backend/src/common/interceptors/transform.interceptor.ts`**: skip wrap khi `headersSent` (cho SSE)
8. **`backend/package.json`**: thêm script `chatbot:ingest`
9. **`backend/.env.example`**: thêm block ENV chatbot
10. **Frontend** `src/tenant/components/Chatbot/`:
    - `useChatStream.js` — fetch + SSE parser (text/tool_call/tool_result/error/done)
    - `MessageBubble.jsx` — bubble với markdown + tool badges
    - `ChatbotWidget.jsx` — FloatButton + Drawer + starter prompts + mute UX
11. **`TenantLayout.jsx`**: mount `<ChatbotWidget />` global
12. **`index.css`**: `.chatbot-markdown` style cho table/code/list

### Build verification
- Backend: `npx nest build` → exit 0, 8 .js files cho chatbot module
- Frontend: `npx vite build` → exit 0 (1.89 MB bundle, warnings size)

### Cần làm tiếp (out of scope task này)
- Test e2e thực với OPENAI_API_KEY thật
- Tạo bảng `chatbot_usage_daily` trong system DB cho báo cáo cost (đã document trong §4.5)
- Lưu chat history vào tenant DB (hiện chỉ trong memory client)
- Phase 4: action thực thi (deep linking, tạo phiếu thu tự động)
