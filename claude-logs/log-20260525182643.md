# Session log — 20260525182643

## Task
Tối ưu prompt AI LLM chatbot:
1. Câu trả lời tự nhiên hơn, không thừa thông tin
2. Bỏ các cụm "Nếu bạn muốn...", "Bạn có muốn tôi..."
3. Thống nhất xưng hô "bạn" — chatbot phục vụ khách hàng, không phụ thuộc history subscriber

## Files đọc
- `docs/chatbots/chatbot-strategy.md`
- `backend/src/tenant-module/chatbot/chatbot.service.ts`
- `backend/src/tenant-module/chatbot/chatbot-routing.ts`
- `backend/src/tenant-module/chatbot/chatbot-direct-response.ts`
- `backend/src/tenant-module/chatbot/chatbot-doc-access.ts`

## Phân tích vấn đề

### chatbot.service.ts
- `BASE_SYSTEM_PROMPT`: Thiếu quy tắc về xưng hô "bạn", thiếu cấm cụm "Nếu bạn muốn", "Bạn có muốn tôi"
- `buildRouteInstruction()`: Chưa cấm các cụm gợi ý thừa
- `formatFallbackFromToolResult()`: Có cụm "Tôi tìm thấy..." cần làm gọn hơn
- Permission denied message: Có "Nếu cần" cần bỏ

### chatbot-direct-response.ts
- `buildActionUnsupportedMessage()`: Có "Nếu bạn muốn" cần thay
- `renderFirstItems()`: Có "Bạn có thể thêm điều kiện lọc nếu muốn xem chính xác hơn" cần bỏ
- No-data message trong `formatDirectToolResult()`: Có "Bạn có thể thử nhập thêm" cần bỏ

## Thay đổi thực hiện

### chatbot.service.ts
1. `BASE_SYSTEM_PROMPT`: Viết lại hoàn toàn — thêm section "Xưng hô" (luôn dùng "bạn", cấm anh/chị/em/quý khách), thêm danh sách cụm cấm ("Nếu bạn muốn...", "Bạn có muốn tôi...", "Hy vọng điều này giúp ích"...), tách rõ nguyên tắc dữ liệu
2. `buildRouteInstruction()`: Thêm "Không đề nghị hay gợi ý thêm sau khi trả lời xong" vào cả RAG_GUIDE và QUERY_DB; UNCERTAIN sửa câu hỏi lại thành 1 câu ngắn
3. `formatFallbackFromToolResult()`: Bỏ cụm "Tôi đã tìm thấy / Tôi chưa tìm thấy", dùng dạng trực tiếp hơn; bỏ "Xin lỗi" ở đầu error message
4. Permission denied: Bỏ "Nếu cần"

### chatbot-direct-response.ts
1. `buildActionUnsupportedMessage()`: Thay "Hiện tại tôi mới hỗ trợ... Nếu bạn muốn..." → "Chatbot hiện chỉ hỗ trợ... Các thao tác... cần thực hiện trực tiếp..."
2. `renderFirstItems()`: Thay "Bạn có thể thêm điều kiện lọc nếu muốn xem chính xác hơn" → "(Còn X kết quả khác, thử thêm điều kiện lọc để thu hẹp kết quả.)" dạng italic, không hỏi
3. No-data message trong `formatDirectToolResult()`: Bỏ "Bạn có thể thử nhập thêm mã chứng từ..." → chỉ còn "Không tìm thấy dữ liệu phù hợp với yêu cầu..."

## Kết quả
- Hoàn thành tất cả thay đổi
- Verify: grep không còn cụm lỗi trong code (các match còn trong prompt là dạng "cấm dùng cụm X")
